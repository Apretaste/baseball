<div class="row">
    <div class="col">
        <h1><%= player.name %>></h1>
    </div>
</div>

<div class="row">
    <div class ="col l12 m12 s12">
        <table class="striped">
            <% for(var i=0; i < records.length; i++) { var record = records[i]; %>
            <tr>
                <td><%= record.name %></td>
                <td><%= record.value %></td>
            </tr>
            <% }%>
        </table>
    </div>
</div>
