<div class="row">
    <div class="col">
        <h1>Juegadores de <%= team.name %></h1>
    </div>
</div>

<% for(i=0; i<players.length;i++) { var player = players[i]; %>
<div class="row">
    <div class="col">
        <%= player.name %>
    </div>
</div>
<% } %>
